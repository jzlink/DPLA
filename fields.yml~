# delimiter: New line, 2 spaces, dash, space. "\n  - example".
#fields of interest from DPLA in json. format
#attributes:
#source               : DPLA or Provider
#conditional sub-field: list of conditional sub fields, if any
#contitional parent-field: parent field if field is a conditional sub-field
#required             : 1/0 indicator of status 1 = required 0 = recommended
#present              : True/False if the field is present in the record
#valid                : True/False if field is valid by our standards
#status               : numerical idicator of field status. see 
#		        fieldStatusCodebook.yml for numerical assignments

#YAML

aggregatedCHO:
   source:
      DPLA
   required:
      1
   present:
   valid:
   status:

id:
   source:
      DPLA
   required:
      1
   present:
   valid:
   status:

originalRecord:
   source:
      DPLA
   required:
      1
   present:
   valid:
   status:

dataProvider:
   source:
      partner
   required:
      1
   present:
   valid:
   status:

isShownAt:
   source:
      partner
   required:
      1
   present:
   valid:
   status:

object:
   source:
      partner
   required:
      1
   present:
   valid:
   status:

provider:
   source:
      partner
   required:
      1
   present:
   valid:
   status:

sourceResource.title:
   source:
      partner
   required:
      1
   present:
   valid:
   status:

sourceResource.rights:
   source:
      partner
   required:
      1
   present:
   valid:
   status:

sourceResource.collection:
   source:
      partner
   conditional sub-fields:
     - sourceResource.collection.title
     - sourceResource.collection.description
   required:
      0
   present:
   valid:
   status:

sourceResource.date:
   source:
      partner
   conditional sub-fields:
     - sourceResource.date.displayDate
   required:
      0
   present:
   valid:
   status:

sourceResource.spatial:
   source:
      partner
   conditional sub-fields:
     - sourceResource.spatial.name
   required:
      0
   present:
   valid:
   status:

sourceResource.creator:
   source:
      partner
   required:
      0
   present:
   valid:
   status:

sourceResource.format:
   source:
      partner
   required:
      0
   present:
   valid:
   status:

sourceResource.language:
   source:
      partner
   required:
      0
   present:
   valid:
   status:

sourceResource.publisher:
   source:
      partner
   required:
      0
   present:
   valid:
   status:

sourceResource.subject:
   source:
      partner
   required:
      0
   present:
   valid:
   status:

sourceResource.type:
   source:
      partner
   required:
      0
   present:
   valid:
   status:

sourceResource.date.displayDate:
   source:
      partner
   confitional parent-field:
      date
   required:
      1
   present:
   valid:
   status:

sourceResource.spatial.name:
   source:
      partner
   confitional parent-field:
      spatial
   required:
      1
   present:
   valid:
   status:

sourceResource.collection.title:
   source:
      partner
   confitional parent-field:
      collection
   required:
      0
   present:
   valid:
   status:

sourceResource.collection.description:
   source:
      partner
   confitional parent-field:
      collection
   required:
      0
   present:
   valid:
   status:
